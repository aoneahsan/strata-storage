import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.stratastorage',
  appName: 'Strata Storage Test',
  webDir: 'build'
};

export default config;
